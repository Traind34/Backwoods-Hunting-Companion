# Backwoods Planner Co.

Backwoods Planner Co. hunting-property planning app.

## Current test build

The current working build is **V5.10**.

The repository has been intentionally restored to the V5.10 baseline. This commit only triggers the connected Vercel deployment; no application code is changed.

## Deployment target

This repository is intended to be deployed with Vercel using the GitHub repository as the source.
