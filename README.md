# Backwoods Planner Co.

Backwoods Planner Co. hunting-property planning app.

## Current build

The app now includes the V8.2 cross-device cloud sync foundation. Local storage remains the offline cache; connected devices can use the same Backwoods sync code once the Vercel database resource is connected.

## Deployment target

This repository is deployed with Vercel using GitHub as the source.
